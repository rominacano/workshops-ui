const BASE_URL = 'http://localhost:3001';
const API_PATH = `${BASE_URL}/api`;

export const CUSTOMERS_PATH = `${API_PATH}/customers`;
export const CARS_PATH = `${API_PATH}/cars`;
export const ORDERS_PATH = `${API_PATH}/repairs`;
